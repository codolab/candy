{
  "packages": ["packages/*"],
  "version": "independent",
  "npmClient": "yarn",
  "useWorkspaces": true,
  "command": {
    "publish": {
      "ignoreChanges": ["*.md", "test/**"],
      "allowBranch": "main"
    }
  },
  "ignoreChanges": [
    "*.md"
  ]
}