import { system } from "candy-system";

export const config = {
  flex: true,
  flexDirection: true,
  flexWrap: true,
  flexGrow: true,
  flexShrink: true,
  order: true,
};

export const flexBox = system(config);
export default flexBox;
